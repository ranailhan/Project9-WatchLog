@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Erişim Reddedildi";
}

<div class="wl-auth-page">
    <div class="text-center" style="max-width:500px;">
        <div style="font-size:5rem;color:var(--primary);margin-bottom:1rem;">
            <i class="fas fa-lock"></i>
        </div>
        <h1 style="font-size:2rem;font-weight:800;color:var(--cream);">Erişim Reddedildi</h1>
        <p class="text-muted mb-4">Bu sayfayı görüntülemek için yetkiniz yok.</p>
        <a asp-controller="Home" asp-action="Index" class="wl-btn-primary">
            <i class="fas fa-home me-2"></i>Ana Sayfaya Dön
        </a>
    </div>
</div>
